import imp
import os
import sys

from aivaid.wsgi import application
# sys.path.insert(0, os.path.dirname(__file__))

# wsgi = imp.load_source('wsgi', 'aivaid.com/aivaid')
# application = wsgi.application
